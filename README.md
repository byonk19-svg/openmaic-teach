# OpenMAIC Teach snapshot

Private archival snapshot of the local OpenMAIC teaching workspace, including
the primary worktree and two experimental worktrees as independent source
directories.

Local credentials, generated dependencies, runtime output, and Codex session
state are intentionally excluded.
